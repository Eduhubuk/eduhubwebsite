# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# editors
.vscode/
.idea/
*.swp

# logs
*.log
npm-debug.log*

# dependencies (none today — the site has no build step)
node_modules/

# environment / secrets — never commit these
.env
.env.*
!.env.example
*.pem
*.key
secrets.json

# deployment
.vercel
